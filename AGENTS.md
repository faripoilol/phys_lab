# Project Policy

## Python Environment
- Use `~/envs/env312` as the required Python environment for this project.
- Activate it before running project commands:

```bash
source ~/envs/env312/bin/activate
```

- Run Python and pip commands from this environment (e.g., `python`, `pip install -r requirements.txt`).
